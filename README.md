# Python-Calculator
This calculator is a command-line based application where users can select an operation from a menu and input two numbers. Based on the selected operation, the program processes the input and displays the result.
